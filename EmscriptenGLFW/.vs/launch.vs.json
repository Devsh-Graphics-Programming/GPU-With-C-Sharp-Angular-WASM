{
  "version": "0.2.1",
  "defaults": {},
  "configurations": [
    {
      "type": "python",
      "project": "run.py ",
      "name": "Launch on localhost",
      "externalConsole": false,
      "stopOnEntry": true
      //"env": { "file_dir": "${configuration.name}" } some macro that expands to where project's build dir is 
    }
  ]
}